# Matura AI frontend

Vue 3 and Vite client for the Matura AI API. Run `npm ci` and `npm run dev` from this
directory. It uses `http://127.0.0.1:8000/api/v1` by default; override that address with
`VITE_API_URL`.
